package com.fourtwosix.mono.tests.data.relational;

import junit.framework.TestCase;

import java.util.logging.Level;
import java.util.logging.Logger;

/**
 * Unit tests for the ResultList.
 */
public class ResultListTestCase extends TestCase {
    private static Logger log = Logger.getLogger(ResultListTestCase.class.getName());

    public void addTests() {
        try {

        }
        catch(Exception e) {
            log.log(Level.SEVERE, "Error during addTests.", e);
        }
    }
}
